# Copier en .env et adapter les valeurs (surtout en production).

# Clé secrète pour signer les cookies de session (obligatoire en prod)
SECRET_KEY=change-me-in-production

# Identifiants de connexion (sans BDD)
LOGIN_USERNAME=demo
LOGIN_PASSWORD=demo
